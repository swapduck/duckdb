# name: benchmark/swap/one_skewed_partition
# description: Skewed external hash join swap benchmark
# group: [one_skewed_partition]

name skewed_join_swap
group join

cache swap_skew

retry load 3

load benchmark/swap/one_skewed_partition/load.sql

run benchmark/swap/one_skewed_partition/q01.sql